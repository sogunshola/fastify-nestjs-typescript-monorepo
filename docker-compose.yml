version: '3.8'
services:
  build:
    build: ./packages/api
    container_name: monorepo-api
    restart: always
    env_file:
      - .env
    ports:
      - 3000:3000
    volumes:
      - .:/usr/src/app
    networks:
      - api-network

networks:
  api-network:


    